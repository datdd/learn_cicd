def prepare_build_enviroment(Map args) {
    println("${args.label}: ${args.str}")

    // params.each { name, value ->
    //     println "Name: $name -> Value: $value"
    // }

    // println("END!")
}

return this